function setup() {
    createCanvas(400,600)
}
function calculate() {
    let genotypes = document.getElementById("genotypes").value
    let box_amount = genotypes.length*7;
    let 
    console.log(genotypes + box_amount)}
